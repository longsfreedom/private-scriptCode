# 流放之路自动挂贪婪脚本技术方案

## 1. 文档目标

本文档用于把项目落地路径重新收紧，避免技术方案与真实可行性脱节。

本文档重点回答以下问题：

1. 第一阶段到底应该先验证什么
2. 架构如何设计才能支持逐步扩展
3. 状态机需要补哪些安全状态
4. 巡逻、祭坛识别和恢复逻辑如何收缩到可实现范围
5. 开发顺序和任务清单应该如何调整

---

## 2. 总体策略调整

相比原方案，新的技术策略做 4 个调整：

1. 先做平台可行性验证，不直接进入业务开发
2. 把 MVP 从“完整多轮闭环”收缩为“稳定进图 + 稳定交互一次祭坛”
3. 状态机增加未知状态和人工接管状态
4. 恢复策略从“尽量拉回主流程”改为“优先安全停机”

原因很明确：

1. 截图和输入链路不稳定时，后面所有模块都会反复返工
2. 祭坛交互是核心业务目标，比拾取和存仓更值得优先验证
3. 状态识别置信度不足时继续点击，风险会快速放大
4. 这类项目最怕不是功能缺失，而是流程失控

---

## 3. 设计原则

第一版建议严格遵循以下原则：

1. **先验证平台，再验证业务**
2. **先固定环境，再谈兼容**
3. **先稳定进图，再做地图内目标**
4. **每一步必须有成功校验**
5. **状态不确定时优先停机**
6. **所有高风险动作必须有重试上限**
7. **日志、截图和模板数据从第一天开始积累**

第一版不追求“智能”，而追求以下能力：

1. 可复现
2. 可校验
3. 可回放
4. 可止损

---

## 4. 分阶段开发路线

### 4.1 M0：平台可行性阶段

目标：

确认当前设备和当前配置下，截图、输入和基础识别可以支撑继续开发。

包含内容：

1. 窗口截图
2. ROI 裁剪
3. 模板匹配
4. OCR 小范围验证
5. 鼠标键盘输入
6. 日志和截图落盘

验收：

1. 固定模板识别成功率达到目标
2. 固定按钮点击成功率达到目标
3. OCR 在固定区域达到可用水平
4. 连续运行不发生明显漂移

### 4.2 M1：稳定进图阶段

目标：

实现从藏身处到进入地图的稳定链路。

包含内容：

1. 场景识别
2. 地图装置识别
3. 放图
4. 放圣甲虫
5. 激活地图
6. 识别传送门并进入

验收：

1. 连续成功进图 20 次
2. 任意失败都能在有限重试后停止

### 4.3 M2：祭坛交互阶段

目标：

实现基础巡逻、候选发现、靠近和祭坛交互。

包含内容：

1. 固定巡逻策略
2. 卡住检测
3. 祭坛候选识别
4. 可交互确认
5. 选项 OCR
6. 规则选择

验收：

1. 多数轮次能完成一次有效祭坛交互
2. 搜索失败时能结束当前轮并安全退出

### 4.4 M3：安全收尾阶段

目标：

实现祭坛交互后的安全回城或安全停机。

包含内容：

1. 回城动作
2. 藏身处校验
3. 恢复管理器
4. 人工接管机制

验收：

1. 失败场景不会进入死循环
2. 关键异常能落到人工接管状态

### 4.5 M4：完整闭环阶段

目标：

在前 3 个阶段足够稳定后，再扩展为单轮完整闭环。

包含内容：

1. 掉落白名单拾取
2. 存仓
3. 下一轮启动

验收：

1. 能连续多轮完整运行
2. 所有关键链路成功率可量化

---

## 5. 整体架构

建议整体采用如下分层：

1. 调度层
2. 状态层
3. 识别层
4. 行为层
5. 策略层
6. 监控层
7. 标定与数据层

### 5.1 调度层

职责：

1. 驱动主循环
2. 管理轮次生命周期
3. 调用状态机
4. 响应停止和人工接管

要求：

1. 不直接做识图
2. 不直接做点击
3. 只负责流程编排

### 5.2 状态层

职责：

1. 维护当前状态
2. 维护状态上下文
3. 管理状态切换
4. 管理超时和重试次数

要求：

1. 状态切换必须记录
2. 低置信度状态不能直接触发危险动作
3. 未知状态必须有明确出口

### 5.3 识别层

职责：

1. 截图
2. 区域裁剪
3. 模板匹配
4. OCR
5. 小地图规则提取
6. 识别结果标准化

要求：

1. 只回答“看到了什么”
2. 不直接决定“下一步做什么”

### 5.4 行为层

职责：

1. 鼠标移动
2. 鼠标点击
3. 鼠标拖拽
4. 键盘按键
5. 等待和轮询

要求：

1. 所有动作可复用
2. 所有动作可超时
3. 所有动作可重试
4. 所有动作可校验

### 5.5 策略层

职责：

1. 巡逻规则
2. 祭坛选项规则
3. 掉落规则
4. 回城规则
5. 存仓规则

要求：

1. 策略尽量配置化
2. 第一版不写复杂评分模型

### 5.6 监控层

职责：

1. 运行日志
2. 状态变化记录
3. 关键截图保存
4. 识别结果落盘
5. 异常告警

### 5.7 标定与数据层

这是原方案里缺失但必须补上的一层。

职责：

1. ROI 标定
2. 模板采集
3. OCR 样本管理
4. 调试截图归档
5. 版本变更后的回归对比

---

## 6. 推荐目录结构

```text
scriptCode/
  docs/
    need.md
    technical.plan.md
  config/
    app.config.json
    resolution.config.json
    altar.rules.json
    patrol.config.json
    recovery.config.json
    loot.rules.json
  assets/
    templates/
      ui/
      map-device/
      altar/
      portal/
      stash/
    samples/
      ocr/
      failures/
    debug/
  tools/
    capture_roi.py
    collect_template.py
    replay_case.py
  src/
    main.py
    core/
      scheduler.py
      state_machine.py
      context.py
      guards.py
    vision/
      screenshot.py
      roi.py
      matcher.py
      ocr.py
      minimap.py
      detectors.py
    input/
      mouse.py
      keyboard.py
      actions.py
    modules/
      env.module.py
      hideout.module.py
      map_device.module.py
      portal.module.py
      patrol.module.py
      altar.module.py
      return.module.py
      loot.module.py
      stash.module.py
      recovery.module.py
    strategies/
      patrol.strategy.py
      altar.strategy.py
      loot.strategy.py
    utils/
      logger.py
      timer.py
      retry.py
      image.py
      metrics.py
```

目录设计原则：

1. 识别、动作、流程、配置分开
2. 模板、样本、调试截图单独管理
3. 从一开始就保留标定和回放工具

---

## 7. 状态机设计

### 7.1 状态列表

第一版建议使用以下状态：

1. `BOOTSTRAP`
2. `CHECK_ENV`
3. `IN_HIDEOUT`
4. `OPEN_MAP_DEVICE`
5. `PLACE_MAP`
6. `PLACE_SCARAB`
7. `ACTIVATE_MAP`
8. `ENTER_PORTAL`
9. `WAIT_MAP_LOAD`
10. `MAP_PATROL`
11. `ALTAR_APPROACH`
12. `ALTAR_INTERACT`
13. `ALTAR_CHOOSE`
14. `RETURN_HIDEOUT`
15. `RECOVERY`
16. `UNKNOWN`
17. `MANUAL_REQUIRED`
18. `STOPPED`

### 7.2 主流程

`BOOTSTRAP -> CHECK_ENV -> IN_HIDEOUT -> OPEN_MAP_DEVICE -> PLACE_MAP -> PLACE_SCARAB -> ACTIVATE_MAP -> ENTER_PORTAL -> WAIT_MAP_LOAD -> MAP_PATROL -> ALTAR_APPROACH -> ALTAR_INTERACT -> ALTAR_CHOOSE -> RETURN_HIDEOUT -> IN_HIDEOUT`

### 7.3 异常流程

建议改成以下流转：

1. 轻微失败：`当前状态 -> RECOVERY -> 当前状态`
2. 状态不确定：`当前状态 -> UNKNOWN -> RECOVERY`
3. 恢复失败：`RECOVERY -> MANUAL_REQUIRED`
4. 用户确认停止：`MANUAL_REQUIRED -> STOPPED`

### 7.4 状态机必须补充的约束

1. 任意状态都必须有超时
2. 任意状态都必须有最大重试次数
3. `UNKNOWN` 不能直接跳回业务状态，必须先经过恢复判断
4. `MANUAL_REQUIRED` 后不再自动执行危险动作
5. 关键动作前必须重新确认场景

### 7.5 状态上下文

建议统一维护上下文对象，至少包含以下字段：

1. 当前状态
2. 上一状态
3. 当前轮次编号
4. 当前状态起始时间
5. 当前状态重试次数
6. 当前轮累计失败次数
7. 当前场景置信度
8. 当前截图路径
9. 地图是否已进入
10. 祭坛是否已成功交互
11. 是否进入人工接管

---

## 8. 识别系统设计

### 8.1 输入源

第一版只依赖以下输入源：

1. 游戏窗口截图
2. 指定区域截图
3. 小地图区域截图
4. 固定区域 OCR

### 8.2 识别方法优先级

建议严格按以下顺序落地：

1. 固定 UI 用模板匹配
2. 固定文本用 OCR
3. 小地图和场景目标用颜色、区域和规则法
4. 只有上述方式确实不够时，才考虑更重的视觉方案

### 8.3 关键识别对象

#### 藏身处阶段

1. 藏身处状态标记
2. 地图装置
3. 仓库

#### 地图装置阶段

1. 地图槽位
2. 圣甲虫槽位
3. 激活按钮
4. 传送门是否生成

#### 地图内阶段

1. 已入图状态
2. 小地图轮廓
3. 祭坛候选视觉特征
4. 可交互提示
5. 祭坛选项文本

### 8.4 识别接口建议

```python
class DetectionResult:
    found: bool
    confidence: float
    bbox: tuple[int, int, int, int] | None
    text: str | None
    extra: dict
```

### 8.5 识别系统新增要求

1. 所有识别结果必须带置信度
2. 关键识别应允许多信号交叉确认
3. OCR 结果必须保留原图和预处理图
4. 无法确认时返回“不可信”，而不是硬给结论

---

## 9. 行为系统设计

### 9.1 行为原则

1. 每个动作必须可复用
2. 每个动作必须可超时
3. 每个动作必须可校验
4. 每个动作必须可限制重试次数
5. 每个动作必须支持随机微偏移
6. 每个动作必须支持节流

### 9.2 基础动作

建议封装以下动作：

1. `move_mouse_to_target`
2. `left_click`
3. `right_click`
4. `double_click`
5. `drag_from_to`
6. `press_key`
7. `hold_key`
8. `wait_until`
9. `capture_and_verify`

### 9.3 复合动作

建议封装以下业务动作：

1. `open_map_device`
2. `place_map`
3. `place_scarab`
4. `activate_map`
5. `enter_portal`
6. `start_patrol`
7. `interact_altar`
8. `choose_altar_option`
9. `cast_town_portal`

### 9.4 行为校验要求

1. 点击地图装置后必须校验界面已打开
2. 放图后必须校验地图已在槽位中
3. 开图后必须校验传送门已生成
4. 点击祭坛后必须校验祭坛界面已出现
5. 回城后必须校验已回到藏身处

---

## 10. 巡逻方案

### 10.1 第一版为什么不做智能寻路

完整寻路需要同时解决：

1. 动态地图理解
2. 障碍和通路识别
3. 移动反馈修正
4. 战斗干扰
5. 搜索覆盖率评估

这个复杂度对第一版过高，因此第一版只做半规则巡逻。

### 10.2 第一版巡逻策略

建议采用固定模式巡逻：

1. 入图后向主方向推进
2. 每隔固定时间切换方向
3. 周期性扫描小地图和屏幕中心
4. 检测到卡住时执行脱困动作
5. 检测到祭坛候选时立刻切换到祭坛状态

### 10.3 巡逻参数

以下参数必须配置化：

1. 单次移动时长
2. 转向间隔
3. 屏幕扫描频率
4. 小地图扫描频率
5. 最大搜索时长
6. 卡住判定阈值
7. 脱困动作序列

### 10.4 卡住检测

建议组合以下信号：

1. 小地图局部图像长时间变化很小
2. 屏幕中心特征重复度过高
3. 连续移动后目标区域无明显变化

触发后执行：

1. 停止当前动作
2. 后退
3. 侧移
4. 随机转向
5. 再次推进

---

## 11. 贪婪祭坛识别与交互方案

### 11.1 候选发现

第一版可以组合以下方式：

1. 屏幕中祭坛视觉模板
2. 小地图关键特征
3. 可交互提示文本

### 11.2 交互流程

建议流程如下：

1. 发现候选祭坛
2. 靠近候选区域
3. 再次确认可交互信号
4. 执行交互点击
5. 校验祭坛界面已打开
6. OCR 读取选项文本
7. 按规则选择目标选项
8. 校验选择结果

### 11.3 选项规则

第一版建议使用简单配置，不做复杂评分：

```json
{
  "prefer_keywords": ["贪婪", "层数", "掉落"],
  "avoid_keywords": ["危险", "减益"]
}
```

### 11.4 交互失败原则

1. 界面未打开时不进入 OCR
2. OCR 结果置信度过低时不盲选
3. 连续失败达到阈值后结束当前轮

---

## 12. 回城、拾取和存仓方案

### 12.1 第一版范围

第一版只要求：

1. 祭坛交互成功后能回城
2. 回城失败时能停机

### 12.2 延后到完整闭环阶段的内容

以下内容建议在 M4 再做：

1. 白名单拾取
2. 背包满判断
3. 存仓
4. 下一轮启动

### 12.3 为什么延后

1. 这些能力不影响验证核心业务目标
2. 它们会显著增加状态复杂度
3. 会让第一版很难定位核心问题到底出在进图、巡逻还是收尾

---

## 13. 异常恢复设计

### 13.1 恢复原则

1. 先轻恢复
2. 再中等恢复
3. 恢复仍失败则进入人工接管
4. 不允许无限重试
5. 状态不确定时不继续危险动作

### 13.2 轻恢复

用于短暂异常：

1. 重新截图
2. 再识别一次
3. 微调鼠标位置
4. 单次动作重试

### 13.3 中等恢复

用于状态可能偏移：

1. 关闭当前界面
2. 回到角色可控制状态
3. 重新识别场景
4. 从最近稳定状态重新开始

### 13.4 重恢复

用于流程明显失控：

1. 尝试回城
2. 回藏身处
3. 重置当前轮
4. 若仍失败则进入人工接管

### 13.5 必须直接进入人工接管的场景

1. 连续多次无法判断当前状态
2. 截图链路异常
3. 输入链路异常
4. 出现未知界面或未知弹窗
5. 恢复次数达到上限

### 13.6 恢复接口建议

```python
class RecoveryManager:
    def try_light_recovery(self, reason: str) -> bool:
        ...

    def try_medium_recovery(self, reason: str) -> bool:
        ...

    def try_heavy_recovery(self, reason: str) -> bool:
        ...
```

---

## 14. 日志、截图和数据沉淀

### 14.1 必须记录的内容

1. 每次状态切换
2. 每次识别结果和置信度
3. 每次点击和按键
4. 每次失败原因
5. 每次恢复动作
6. 每轮耗时和结果

### 14.2 必须保存的截图

1. 状态切换前后截图
2. 识别失败截图
3. 祭坛候选截图
4. 祭坛 OCR 截图
5. 回城失败截图
6. 未知状态截图

### 14.3 必须保留的数据资产

1. 模板原图
2. ROI 标定参数
3. OCR 样本图
4. 失败案例回放数据

---

## 15. MVP 开发顺序

### 第 1 步：平台可行性

包含内容：

1. 窗口截图
2. 模板匹配
3. OCR 预处理
4. 鼠标键盘输入
5. 标定工具
6. 日志系统

完成标准：

1. 平台验证项通过

### 第 2 步：状态机骨架

包含内容：

1. 状态定义
2. 上下文对象
3. 超时机制
4. 重试机制
5. 人工接管机制

完成标准：

1. 能完整记录状态流转

### 第 3 步：地图装置链路

包含内容：

1. 地图装置识别
2. 放图
3. 放圣甲虫
4. 开图
5. 入图

完成标准：

1. 能连续成功进图

### 第 4 步：地图内移动链路

包含内容：

1. 基础巡逻
2. 卡住检测
3. 搜索超时机制

完成标准：

1. 能在地图内稳定移动

### 第 5 步：祭坛链路

包含内容：

1. 候选识别
2. 靠近
3. 交互
4. 选项 OCR
5. 选项选择

完成标准：

1. 能在多数轮次中完成一次有效交互

### 第 6 步：安全收尾

包含内容：

1. 回城
2. 恢复管理器
3. 人工接管

完成标准：

1. 失败时不会进入死循环

### 第 7 步：完整闭环

包含内容：

1. 掉落拾取
2. 存仓
3. 下一轮开始

完成标准：

1. 能连续多轮运行

---

## 16. 首版开发任务清单

### 16.1 P0 任务

1. 实现窗口截图
2. 实现 ROI 标定工具
3. 实现模板匹配
4. 实现 OCR 封装和预处理
5. 实现日志和截图落盘
6. 实现状态机和上下文
7. 实现鼠标键盘控制
8. 实现地图装置模块
9. 实现入图模块
10. 实现基础巡逻模块
11. 实现祭坛识别与交互模块
12. 实现回城模块
13. 实现恢复管理器
14. 实现人工接管机制

### 16.2 P1 任务

1. 增强卡住检测
2. 增强 OCR 稳定性
3. 增加失败案例回放
4. 增加多信号状态判定
5. 增加白名单拾取

### 16.3 P2 任务

1. 实现存仓
2. 实现多轮闭环
3. 实现自动采购
4. 实现库存管理
5. 实现多配置支持

---

## 17. 现在最应该先做的事

如果要立刻开始开发，第一周建议只聚焦以下 5 件事：

1. **做截图和输入链路验证**
2. **做模板采集和 ROI 标定工具**
3. **做日志和状态机骨架**
4. **做地图装置操作链路**
5. **验证能否稳定进图**

原因：

1. 进图不稳定，地图内逻辑无法验证
2. 标定和模板基础不稳，后面识别会一直返工
3. 没有日志和截图，失败原因无法定位

---

## 18. 最终建议

这类项目最容易犯的错误是：

1. 一开始就追求全自动
2. 一开始就追求智能寻路
3. 一开始就把拾取、回城、存仓和补货全塞进第一版
4. 一开始就假设异常都能自动恢复

更稳的做法是：

1. 先做平台可行性验证
2. 再把进图能力做稳
3. 再把祭坛交互做稳
4. 再把异常止损做稳
5. 最后再扩成完整闭环

一句话总结：

**第一阶段的目标不是“全自动刷图”，而是“稳定进图并稳定完成一次祭坛交互”。**
