# 开发进度记录

## 文档用途

这个文档用于长期维护项目真实开发进度，解决以下问题：

1. 当前实际开发到了哪里
2. 哪些功能已经可用，哪些还只是骨架或占位
3. 当前阻塞点是什么
4. 下一次继续开发时应该从哪里接手

建议把本文件作为 `technical.plan.md` 的配套执行文档：

1. `technical.plan.md` 负责记录总体方案和阶段目标
2. 本文件负责记录真实实现状态和下一步编写计划

---

## 维护规则

每次开发结束后，至少更新以下内容：

1. `最后更新时间`
2. `本次开发摘要`
3. `当前阶段`
4. `已实现能力`
5. `未完成项`
6. `下一步功能编写`
7. `风险与阻塞`

建议更新顺序：

1. 先写本次新增内容
2. 再修正当前状态判断
3. 最后更新下一步计划

---

## 基本信息

- 最后更新时间：`2026-05-19`
- 当前阶段：`M0：平台可行性阶段`
- 当前子目标：`补齐真实 OCR，并准备进入第一个真实业务状态 IN_HIDEOUT`
- 当前状态总结：`截图、输入、ROI、模板采集、模板匹配基础链路已具备，OCR 和 M1 业务状态仍未落地`
- 对齐方案文档：[technical.plan.md](file:///c:/Users/12207/Desktop/scriptCode/technical.plan.md)

---

## 本次开发摘要

- 新增了 Windows 真实截图第一版，支持按窗口标题关键词抓取客户区截图。
- 新增了 Windows 真实输入第一版，支持点击和按键发送，并保留调试日志。
- 新增了 ROI 标定、ROI 裁切、模板采集工具骨架。
- 新增了模板注册表、ROI 仓库和纯 Python 模板匹配第一版。
- 新增了长期维护用的开发进度文档，用于后续持续更新。

---

## 当前阶段判断

当前处于 `M0：平台可行性阶段`，正在从“DryRun 骨架”切换到“Windows 真实链路”。

当前判断依据：

1. 已完成截图链路
2. 已完成输入链路
3. 已完成 ROI 标定与模板采集工具
4. 已完成模板匹配基础版本
5. 真实 OCR 未完成
6. `IN_HIDEOUT` 等真实业务状态还未接入

阶段结论：

**当前已经完成 M0 的大部分基础设施，但还没有完成 M0 验收，原因是 OCR 和真实模板数据验证尚未补齐。**

---

## 已实现能力

### 启动与调度

- 入口文件：`src/main.py`
- 调度器：`src/core/scheduler.py`
- 状态上下文：`src/core/context.py`
- 状态机骨架：`src/core/state_machine.py`
- 超时与重试守卫：`src/core/guards.py`
- 恢复管理器骨架：`src/modules/recovery_module.py`

当前能力：

1. 可以完成启动和环境检查流程
2. `dry_run` 模式下可以完整跑通占位状态
3. 调度层已具备后续挂接真实模块的结构

### 运行时装配

- 运行时工厂：`src/core/runtime_factory.py`
- 项目路径工具：`src/core/pathing.py`

当前能力：

1. 支持 `dry_run` 和 `windows` 模式切换
2. 能统一装配截图、输入、模板匹配、OCR 提供器
3. 真实实现可以逐项替换，不需要改主入口结构

### Windows 真实截图

- Win32 工具层：`src/utils/windows_api.py`
- 截图提供器：`src/vision/contracts.py` 中的 `WindowsScreenshotProvider`

当前能力：

1. 可按窗口标题关键词查找目标窗口
2. 可抓取客户区截图并保存到 `runtime/screenshots`
3. 找不到窗口时可安全失败并返回明确错误

### Windows 真实输入

- 输入控制器：`src/input/contracts.py` 中的 `WindowsInputController`

当前能力：

1. 支持基于客户区坐标执行左键点击
2. 支持发送单次键盘按键
3. 支持将输入动作写入 `runtime/debug`

当前限制：

1. 还没有鼠标移动轨迹
2. 还没有双击、拖拽、按住、节流、随机偏移
3. 还没有动作前后场景校验

### ROI 标定与模板采集

- ROI 仓库：`src/vision/roi.py`
- ROI 标定工具：`tools/calibrate_roi.py`
- ROI 裁切工具：`tools/capture_roi.py`
- 模板采集工具：`tools/collect_template.py`
- ROI 配置：`config/roi.config.json`

当前能力：

1. 可写入和更新 ROI 配置
2. 可按 ROI 从 BMP 截图中裁切区域
3. 可按 ROI 从截图中裁出模板文件

### 模板匹配第一版

- BMP 处理：`src/vision/bitmap.py`
- 模板注册表：`src/vision/template_registry.py`
- 模板匹配器：`src/vision/contracts.py` 中的 `WindowsTemplateMatcher`
- 模板配置：`config/templates.config.json`

当前能力：

1. 可从配置中读取模板路径、阈值、绑定 ROI
2. 可对 BMP 图像执行基础模板匹配
3. ROI 越界时可返回安全失败结果

当前限制：

1. 当前是纯 Python 暴力匹配，性能一般
2. 更适合小 ROI、小模板验证
3. 还没有引入 OpenCV 或更高效算法
4. 还没有做多信号交叉确认

### 日志与验证

- 日志工具：`src/utils/logger.py`

当前能力：

1. 可记录运行模式
2. 可记录环境验证结果
3. 可记录截图链路和输入链路错误
4. 可输出下一步建议

---

## 已验证通过项

已做过的验证：

1. `dry_run` 模式可完整跑通占位状态流转
2. `windows` 模式在找不到目标窗口时会安全失败，不会直接崩溃
3. 前台窗口截图链路已验证可落 BMP
4. ROI 标定工具可正常写入 ROI 配置
5. ROI 裁切工具可正常输出裁切 BMP
6. 模板匹配器在 ROI 越界时可返回安全失败结果

当前仍未完成的验证：

1. 真实目标窗口截图连续稳定性验证
2. 真实目标窗口输入成功率验证
3. 真实模板识别成功率验证
4. 真实 OCR 可用性验证

---

## 未完成项

以下内容尚未完成，当前仍为空白、占位或仅有骨架：

1. 真实 OCR 实现
2. OCR 预处理图保存
3. `IN_HIDEOUT` 真实状态识别
4. 地图装置真实识别
5. 地图装置交互动作封装
6. 传送门识别与入图
7. 巡逻、祭坛、回城等地图内逻辑
8. 恢复管理器的真实恢复流程
9. 人工接管交互流程

---

## 风险与阻塞

当前主要风险：

1. 模板匹配还没有真实模板数据，当前模板只是占位文件
2. 纯 Python 模板匹配性能有限，后续大概率要升级
3. OCR 尚未接入，导致 M0 还不能完整验收
4. 真实输入虽然已接好，但还没有针对目标游戏窗口做稳定性验证

当前阻塞点：

1. 缺少真实游戏截图样本
2. 缺少真实模板数据
3. 缺少 OCR 依赖和具体接入方案

---

## 当前做到哪里

如果按 [technical.plan.md](file:///c:/Users/12207/Desktop/scriptCode/technical.plan.md) 对齐，当前进度判断如下：

1. `M0` 已完成大部分基础设施
2. 已覆盖截图、输入、ROI、模板采集、模板匹配、日志
3. `M0` 还差真实 OCR 和真实模板验证
4. `M1` 还没有正式开始真实业务链路开发

一句话概括：

**当前已经完成“平台链路基础设施搭建”，下一步应补齐 OCR，并开始第一个真实业务状态 `IN_HIDEOUT`。**

---

## 下一步功能编写

建议后续严格按以下顺序推进。

### 优先级 1：真实 OCR

目标：

1. 接入固定区域 OCR
2. 增加基础预处理
3. 保留原图和预处理图
4. 输出文本和置信度

建议落点：

1. `src/vision/contracts.py`
2. 新增 `src/vision/ocr.py`，或先在现有结构中补实现

完成后预期：

1. M0 的 OCR 验证项开始可测
2. 后续祭坛选项识别也有基础

### 优先级 2：藏身处真实状态识别

目标：

1. 基于 `hideout_anchor` 模板识别当前是否在藏身处
2. 把识别结果接入 `IN_HIDEOUT`
3. 为 `OPEN_MAP_DEVICE` 提供前置校验

建议落点：

1. 新增 `src/modules/hideout_module.py`
2. 调整 `src/core/scheduler.py`
3. 调整 `src/core/state_machine.py`

### 优先级 3：地图装置识别

目标：

1. 识别地图装置位置
2. 识别地图装置面板是否打开
3. 为打开地图装置动作提供校验依据

建议落点：

1. 新增 `src/modules/map_device_module.py`
2. 增加地图装置相关 ROI 和模板

### 优先级 4：动作系统补全

目标：

1. 增加鼠标移动
2. 增加双击
3. 增加拖拽
4. 增加节流和随机微偏移
5. 增加动作后校验

### 优先级 5：M1 首个真实闭环

目标：

1. `IN_HIDEOUT`
2. `OPEN_MAP_DEVICE`
3. 校验地图装置已打开

说明：

只有这条链路打通后，才建议继续做放图、放圣甲虫和开图。

---

## 下一次接手入口

如果下次继续开发，建议直接从以下两条路线中选一条开始：

1. 路线 A：先补真实 OCR，完成 M0 最后一块短板
2. 路线 B：先做 `IN_HIDEOUT` 真实识别，开始 M1 第一个真实状态

默认推荐：

**优先走路线 A，先补 OCR，再做 `IN_HIDEOUT`。**

---

## 更新模板

后续每次更新时，可以直接按下面模板改：

```markdown
## 基本信息

- 最后更新时间：`YYYY-MM-DD`
- 当前阶段：`M0 / M1 / M2 / M3 / M4`
- 当前子目标：`一句话说明`
- 当前状态总结：`一句话说明`

## 本次开发摘要

- 新增了什么
- 修复了什么
- 验证了什么

## 已验证通过项

1. ...
2. ...

## 未完成项

1. ...
2. ...

## 风险与阻塞

1. ...
2. ...

## 下一步功能编写

1. ...
2. ...
```
