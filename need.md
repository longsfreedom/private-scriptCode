# 流放之路自动挂贪婪脚本需求文档

## 1. 文档目的

本文档用于重新定义项目范围，避免一开始就把目标定得过大。

本项目的真实目标不是立刻做到“全自动无人值守”，而是按阶段验证以下问题：

1. 当前环境下是否能稳定截图、识别和输入
2. 是否能在固定配置下稳定完成进图
3. 是否能在地图内稳定发现并交互贪婪祭坛
4. 是否能在异常时安全停止，而不是无限误操作
5. 在上述能力稳定后，是否值得继续做拾取、回城、存仓和补货

---

## 2. 核心结论

如果目标是优先把流程真正跑起来，第一版不应该直接做完整刷图闭环，更不应该一开始就做自动采购、复杂寻路和多配置兼容。

更合理的顺序是：

1. 先做平台可行性验证
2. 再做稳定进图
3. 再做地图内祭坛发现与交互
4. 再做安全回城或安全停机
5. 最后才做拾取、存仓、下一轮和补货

项目第一阶段最重要的不是“自动化程度”，而是下面 4 件事：

1. 状态识别是否可信
2. 输入动作是否可控
3. 每一步是否可以校验
4. 失败时是否会立刻止损

一句话总结：

**先证明“稳定进图 + 稳定交互一次祭坛”能成立，再扩成完整刷图闭环。**

---

## 3. 项目边界

### 3.1 最终目标

最终希望脚本在尽量少人工干预的前提下，完成一轮贪婪刷图流程：

1. 放入地图和圣甲虫
2. 开启地图并进入
3. 在地图内巡逻
4. 发现并交互贪婪祭坛
5. 选择目标选项
6. 处理掉落
7. 回城
8. 存仓
9. 返回初始位置准备下一轮

### 3.2 第一版真实边界

第一版只允许在严格固定环境下运行：

1. 固定分辨率
2. 固定窗口模式
3. 固定 UI 缩放
4. 固定语言
5. 固定地图类型
6. 固定圣甲虫组合
7. 固定角色技能方案
8. 固定背包布局
9. 固定仓库页
10. 固定过滤器规则

### 3.3 第一版明确不做

以下内容不纳入第一版承诺范围：

1. 多分辨率兼容
2. 多地图类型兼容
3. 智能全图寻路
4. 自动采购和库存管理
5. 复杂仓库整理
6. 长时间完全无人值守
7. 复杂战斗策略自适应

---

## 4. 必须先验证的前置可行性

在任何业务逻辑开发前，必须先完成可行性验证。只要其中任意一项不成立，后续大模块都应暂停。

### 4.1 平台可行性验证项

1. 能否稳定截取目标窗口画面
2. 截图是否存在黑屏、延迟、缩放偏移或颜色失真
3. 鼠标移动和点击是否能稳定生效
4. 键盘按键和长按是否能稳定生效
5. 游戏窗口焦点切换后是否会导致输入丢失
6. 固定 UI 元素是否能通过模板匹配稳定识别
7. OCR 在固定字体和固定区域下是否有可接受的识别率

### 4.2 可行性验证通过标准

建议至少满足以下标准：

1. 连续截图 200 次无明显黑屏和错位
2. 固定按钮点击成功率达到 95% 以上
3. 固定 UI 模板识别成功率达到 95% 以上
4. 关键 OCR 文本在固定区域下识别成功率达到 90% 以上
5. 连续运行 30 分钟不出现明显状态漂移

### 4.3 可行性验证失败时的处理

如果前置验证不通过，不应继续堆业务模块，而应优先解决以下问题：

1. 截图链路
2. 输入链路
3. 分辨率和 ROI 标定
4. 模板采集质量
5. OCR 截图区域和预处理

---

## 5. 分阶段目标

### 5.1 M0：平台验证阶段

目标：

确认当前机器、当前游戏配置和当前识别方案是否具备继续开发价值。

范围：

1. 窗口截图
2. 鼠标键盘输入
3. 固定 UI 模板识别
4. OCR 小范围验证
5. 基础日志和截图落盘

验收：

1. 前置可行性验证项通过

### 5.2 M1：稳定进图阶段

目标：

实现从藏身处到成功进入地图。

范围：

1. 场景识别
2. 地图装置识别
3. 放图
4. 放圣甲虫
5. 激活地图
6. 识别传送门并进入

验收：

1. 连续成功进图 20 次
2. 失败时不会连续乱点

### 5.3 M2：祭坛交互阶段

目标：

实现进图后找到祭坛并完成一次有效交互。

范围：

1. 地图内基础巡逻
2. 卡住检测
3. 祭坛候选识别
4. 靠近祭坛
5. 打开祭坛界面
6. OCR 识别选项
7. 选择目标选项

验收：

1. 大部分轮次能成功发现祭坛
2. 多数成功发现的轮次能完成一次有效交互
3. 搜索失败时能安全退出当前轮

### 5.4 M3：安全收尾阶段

目标：

实现祭坛交互后的安全回城或安全停机。

范围：

1. 回城
2. 回到藏身处校验
3. 基础异常恢复
4. 停机和人工接管

验收：

1. 失败时能在有限重试后停止
2. 不出现无限循环误操作

### 5.5 M4：完整闭环阶段

目标：

在前 3 个阶段稳定后，再扩展为单轮完整闭环。

范围：

1. 白名单拾取
2. 存仓
3. 下一轮启动

验收：

1. 能连续完成多轮
2. 各环节成功率可量化

---

## 6. 第一版 MVP 重新定义

### 6.1 推荐 MVP 范围

第一版 MVP 不应定义为“完整多轮刷图闭环”，而应定义为下面这条更小的链路：

1. 人工提前准备地图和圣甲虫
2. 脚本自动进图
3. 脚本自动巡逻
4. 脚本自动识别并交互一次贪婪祭坛
5. 成功后自动回城或安全停机

### 6.2 为什么这样定义更合理

1. 进图是所有地图内逻辑的前置条件
2. 祭坛交互才是项目的核心业务目标
3. 拾取和存仓虽然重要，但不是最早的验证重点
4. 先把闭环做小，才能更快暴露核心难点

### 6.3 MVP 成功标准

满足以下条件可视为 MVP 跑通：

1. 能连续成功进图 20 次
2. 能在多数轮次中成功完成一次祭坛交互
3. 每一步都有超时和结果校验
4. 失败时最多进行有限重试
5. 失败后能够安全停机，不无限乱点

---

## 7. 总体流程定义

### 7.1 最终目标流程

1. 启动脚本
2. 检查环境和当前状态
3. 打开地图装置
4. 放入地图和圣甲虫
5. 开图并进图
6. 地图内巡逻和搜索祭坛
7. 发现祭坛并交互
8. 选择目标选项
9. 处理掉落
10. 回城
11. 存仓
12. 准备下一轮

### 7.2 第一版实际流程

1. 启动脚本
2. 检查是否处于可执行环境
3. 打开地图装置
4. 放图和开图
5. 进入地图
6. 按固定巡逻策略搜索祭坛
7. 找到祭坛后完成交互
8. 成功后回城，失败后安全停机

### 7.3 第一版结束条件

满足以下任一条件即结束当前轮：

1. 祭坛已成功交互
2. 地图搜索超时
3. 卡住恢复失败
4. 状态识别连续失败
5. 用户主动停止

---

## 8. 状态机需求

第一版必须采用状态机，不允许使用散乱的 if-else 流程拼接。

### 8.1 推荐状态

1. `BOOTSTRAP`
2. `CHECK_ENV`
3. `IN_HIDEOUT`
4. `OPEN_MAP_DEVICE`
5. `PLACE_MAP`
6. `PLACE_SCARAB`
7. `ACTIVATE_MAP`
8. `ENTER_PORTAL`
9. `WAIT_MAP_LOAD`
10. `MAP_PATROL`
11. `ALTAR_APPROACH`
12. `ALTAR_INTERACT`
13. `ALTAR_CHOOSE`
14. `RETURN_HIDEOUT`
15. `RECOVERY`
16. `UNKNOWN`
17. `MANUAL_REQUIRED`
18. `STOPPED`

### 8.2 必须补充的状态要求

1. 允许进入 `UNKNOWN`，表示当前无法可靠判断所处场景
2. 允许进入 `MANUAL_REQUIRED`，表示脚本不再继续自动操作
3. 任意高风险动作前，必须重新确认当前状态
4. 任意状态连续失败达到阈值后，必须退出主流程

### 8.3 状态切换原则

1. 状态切换必须记录日志
2. 状态切换最好由两个以上信号共同确认
3. 低置信度识别不能直接驱动危险动作
4. 状态不确定时优先停机，不优先硬恢复

---

## 9. 识别与交互需求

### 9.1 识别优先级

建议按以下顺序落地：

1. 固定 UI 使用模板匹配
2. 固定区域文本使用 OCR
3. 小地图和地图内目标使用颜色、区域和规则法
4. 只有前面不够时再考虑更重的视觉方案

### 9.2 关键识别对象

1. 藏身处状态标记
2. 地图装置界面
3. 地图槽位和圣甲虫槽位
4. 激活按钮
5. 传送门
6. 地图内可巡逻状态
7. 贪婪祭坛候选目标
8. 祭坛交互提示
9. 祭坛选项文本

### 9.3 交互要求

1. 每次点击后必须有结果校验
2. 不允许连续无校验点击
3. 动作必须支持超时
4. 动作必须支持重试次数限制
5. 动作必须支持随机微偏移和节流

---

## 10. 地图内巡逻与祭坛需求

### 10.1 巡逻原则

第一版不做智能全图寻路，只做可控的半规则巡逻：

1. 固定主方向推进
2. 定时转向
3. 定时扫描小地图和屏幕中心
4. 检测到卡住时执行脱困动作
5. 检测到祭坛候选后立刻切换交互流程

### 10.2 巡逻必须配置化的参数

1. 单次移动时长
2. 转向间隔
3. 扫描频率
4. 搜索超时时长
5. 卡住判定阈值
6. 脱困动作序列

### 10.3 祭坛交互原则

1. 先发现候选目标
2. 再靠近并确认可交互
3. 确认界面打开后再做 OCR
4. 选项选择必须基于配置规则
5. OCR 不可信时宁可放弃当前轮，也不要盲选

---

## 11. 异常恢复与止损要求

### 11.1 恢复原则

1. 优先轻量恢复
2. 恢复失败后优先停机
3. 不允许无限循环恢复
4. 不允许在未知状态下继续危险动作

### 11.2 必须处理的异常

1. 找不到地图装置
2. 放图失败
3. 激活失败
4. 传送门未生成
5. 入图后未加载完成
6. 地图内长时间卡住
7. 找不到祭坛
8. 祭坛界面未打开
9. OCR 结果不可信
10. 回城失败

### 11.3 止损要求

以下场景必须直接停止并等待人工处理：

1. 连续多次无法判断状态
2. 连续多次恢复失败
3. 出现未知弹窗或未知界面
4. 截图或输入链路异常
5. 当前画面与任何已知模板严重不匹配

---

## 12. 验收指标

建议从第一天开始记录以下指标：

1. 截图成功率
2. 固定模板识别成功率
3. OCR 识别成功率
4. 进图成功率
5. 祭坛发现成功率
6. 祭坛交互成功率
7. 平均单轮搜索时间
8. 卡住恢复成功率
9. 安全停机成功率
10. 连续运行轮数

---

## 13. 风险与前置条件

### 13.1 前置条件

1. 固定显示设置
2. 固定 UI 布局
3. 固定输入映射
4. 固定游戏语言
5. 固定角色技能方案
6. 固定地图和刷图策略
7. 可稳定获取模板和 OCR 样本

### 13.2 主要风险

1. 游戏更新导致模板失效
2. 地图环境变化导致祭坛识别率下降
3. OCR 在特效和滤镜下误读
4. 网络波动导致状态判断失真
5. 长时间运行导致状态漂移
6. 截图或输入链路存在不稳定性

---

## 14. 最终建议

如果现在开始推进，优先顺序应改为：

1. **先做截图、输入、模板识别可行性验证**
2. **再做状态机和日志骨架**
3. **再做地图装置链路，先证明能稳定进图**
4. **再做地图内巡逻和祭坛交互**
5. **最后再做拾取、存仓、下一轮和补货**

一句话总结：

**先把“进图成功”和“祭坛交互成功”做成可重复能力，再追求完整自动化。**
