- ROI 与模板条目均使用语义化键名（如 hideout_anchor、map_device_panel）并以 x/y/width/height 描述矩形区域。
- 每个配置项附带 description 字段说明用途，便于维护与调试。
- 跨文件引用通过相对路径字符串（如 config/templates.config.json、assets/templates）而非硬编码绝对路径。
- 阈值类参数（template_match_threshold、ocr_confidence_threshold、input_success_threshold）集中在 platform_validation 分组中统一管理。